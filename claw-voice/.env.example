# Copy to .env.local and fill in. NEVER commit .env.local (it's gitignored).
# On Fly these are set via `fly secrets set ...` instead of a file.

# --- LiveKit (from your LiveKit Cloud project; same values as on Vercel) ---
LIVEKIT_URL=wss://thomasclaw-8t0mfso9.livekit.cloud
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# --- Smallest.ai (owner-funded shared key; same one the app already uses) ---
SMALLEST_API_KEY=

# --- Phase 1+ : bridge to the Next.js A->B engine (not needed for the spike) ---
VOICE_TURN_URL=https://thomasclaw.vercel.app/api/voice-turn
VOICE_WORKER_SHARED_SECRET=
